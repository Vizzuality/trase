language: ruby
rvm:
  - 2.4.1
services:
  - postgresql
addons:
  postgresql: "9.5"
before_install:
  - gem install bundler
  - bundle install
  - bundle exec rake db:create db:structure:load RAILS_ENV=test

