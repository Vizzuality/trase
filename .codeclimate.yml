engines:
  rubocop:
    enabled: true
  brakeman:
    enabled: true
  bundler-audit:
    enabled: true
  duplication:
    enabled: true
    exclude_paths:
      - app/admin/api/v3/
    config:
      languages:
      - ruby:
  apexmetrics:
    enabled: true
  reek:
    enabled: true
    checks:
      UncommunicativeModuleName:
        enabled: false
      IrresponsibleModule:
        enabled: false
      InstanceVariableAssumption:
        enabled: false

ratings:
  paths:
  - app/**
  - lib/**
  - "**.rb"
exclude_paths:
- spec/**/*
- "**/vendor/**/*"
- db/**/*
- 'lib/db_helpers/**/*' # db helpers, mostly temporary
