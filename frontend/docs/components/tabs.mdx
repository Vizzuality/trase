---
name: Tabs
menu: Components
---
import { PropsTable, Playground } from 'docz'
import { State } from 'react-powerplug';
import Tabs from 'react-components/shared/tabs/tabs.component';
import 'styles/_docz.scss';

# Tabs

<div className="components-container">
  <State initial={{ tabs: ['First', 'Second', 'Third'], selectedTab: 'First' }}>
    {({ state, setState }) => (
      <Tabs
        tabs={state.tabs}
        selectedTab={state.selectedTab}
        onSelectTab={(tab) => setState({selectedTab: tab})}
      />
    )}
  </State>
</div>
<PropsTable of={Tabs} />

<Playground>
  <State initial={{ tabs: ['First', 'Second', 'Third'], selectedTab: 'First' }}>
    {({ state, setState }) => (
      <Tabs
        tabs={state.tabs}
        selectedTab={state.selectedTab}
        onSelectTab={(tab) => setState({selectedTab: tab})}
      />
    )}
  </State>
</Playground>