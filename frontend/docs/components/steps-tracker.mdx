---
name: Steps Tracker
menu: Components
---

import { PropsTable, Playground } from 'docz';
import { State } from 'react-powerplug';
import StepsTracker from 'react-components/shared/steps-tracker';
import Button from 'react-components/shared/button/button.component';
import 'styles/_docz.scss';

## Steps Tracker

<State initial={{ active: 0 }}>
  {({ state, setState }) => (
    <div className="components-container">
      <Button color="charcoal" size="sm" onClick={() => setState({ active: (state.active < 3 ? state.active + 1 : 0) })}>
        Update
      </Button>
      <StepsTracker
        activeStep={state.active}
        steps={[{ label: 'source countries' },{ label: 'countries' }, { label: 'commodities' }, { label: 'companies' }]}
      />
    </div>
  )}
</State>

<PropsTable of={StepsTracker} />

<Playground>
  <State initial={{ active: 0 }}>
    {({ state, setState }) => (
      <div className="components-container">
        <Button color="charcoal" size="sm" onClick={() => setState({ active: (state.active < 2 ? state.active + 1 : 0) })}>
          Update
        </Button>
        <StepsTracker
          activeStep={state.active}
          steps={[{ label: 'countries' }, { label: 'commodities' }, { label: 'companies' }]}
        />
      </div>
    )}
  </State>
</Playground>
