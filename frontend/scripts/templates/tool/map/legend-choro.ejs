<div class="bucket-container js-bucket-legend <%= cssClass %>">
  <div class="bucket-names">
    <div class="layer-name"><%= title[0] %></div>
    <div class="layer-name"><%= title[1] %></div>
  </div>
  <ul class="bucket-legend" data-colors="5">
    <% colors.forEach(function(color, i) { %>
      <li class="bucket-item" >
        <div class="bucket <%= color %>">
          <% if (!isBivariate && typeof bucket !== "undefined" && bucket !== null) { %>
            <span><%= abbreviateNumber(bucket[0][i], 3, i) %></span>
          <% } %>
        </div>
      </li>
    <% }) %>
  </ul>

  <% if (isBivariate && typeof bucket !== "undefined" && bucket[0] !== null && bucket[1] !== null) { %>
    <ul class="bucket-values">
      <% bucket[0].reverse().forEach(function(value) { %>
        <li><%= abbreviateNumber(value, 3) %></li>
      <% }) %>
      <% bucket[1].forEach(function(value) { %>
        <li><%= abbreviateNumber(value, 3) %></li>
      <% }) %>
    </ul>
  <% } %>

  <svg class="icon icon-bidimensional-legend-arrows"><use xlink:href="#icon-bidimensional-legend-arrows"></use></svg>
  <svg class="icon icon-unidimensional-legend-arrows"><use xlink:href="#icon-unidimensional-legend-arrows"></use></svg>

  <ul class="bullets">
    <li><div class="bullet ch-default" style="background:#ebebeb"></div>N/A</li>
    <li><div class="bullet ch-zero" style="background:#ffffff"></div>0</li>
  </ul>
</div>
